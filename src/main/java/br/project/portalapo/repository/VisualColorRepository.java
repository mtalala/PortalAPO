package br.project.portalapo.repository;

import br.project.portalapo.model.VisualColor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VisualColorRepository extends JpaRepository<VisualColor, Long> {
}